import React from 'react'
import { styled, paddingTop, paddingBottom } from 'styles/css'

const PaddedBox = styled.div`
  ${paddingTop('xxxl')};
  ${paddingBottom('xxxl')};
`

export default PaddedBox
